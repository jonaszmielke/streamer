export const FauxFilm = () => (
    <>
        <div
            className="absolute inset-0"
            style={{ background: 'radial-gradient(ellipse at 30% 40%, #2a241a 0%, #0a0907 70%)' }}
        />
        <div
            className="absolute inset-0 opacity-[0.06]"
            style={{
                backgroundImage:
                    'repeating-linear-gradient(90deg, rgba(232,220,196,1) 0 1px, transparent 1px 6px)',
            }}
        />
    </>
)
